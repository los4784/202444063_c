int main() {
    int i, sum = 0;

    printf("정수를 입력하세요: ");
    scanf("%d", &i);

    for (int num = 2; num <= i; num += 2) 
{
        sum += num;
}

    printf("1부터 %d까지의 짝수들의 합: %d\n", i, sum);

    return 0;
}
